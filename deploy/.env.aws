# AWS Configuration
export AWS_REGION="us-east-1"

# Generate secure admin key
export ADMIN_KEY="ksdb_$(openssl rand -hex 32)"

# Database URL (we'll use PostgreSQL RDS or SQLite for now)
# For SQLite (simpler, no cost):
export DATABASE_URL="sqlite:///data/ksdb_cloud.db"

# For PostgreSQL RDS (production):  
# export DATABASE_URL="postgresql://ksdb:password@your-rds-endpoint.rds.amazonaws.com:5432/ksdb"
